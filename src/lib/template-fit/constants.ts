/** Standard print-industry bleed and safe margins, in inches. Must match
 * the website's src/lib/template-fit/constants.ts exactly, or the two
 * platforms' Fit Studio boards would render different guide sizes for the
 * same product. */
export const BLEED_IN = 0.125;
export const SAFE_IN = 0.125;
